Closing a file that was opened with ``memmap=True`` and ``lazy_load=False``
now releases it. Previously the file stayed open until the ``AsdfFile`` object
itself was discarded, even when no array data had been kept.
